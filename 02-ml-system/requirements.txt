# Exact versions, tested together on Python 3.12.
# Regenerate deliberately, never with a loose range: a classroom where two
# students resolve different versions debugs pip instead of ML systems.

boto3==1.43.73
botocore==1.43.73
numpy==2.5.2
scikit-learn==1.9.0
PyYAML==6.0.3
